crisis = {
	fall_of_empire = {
		icon = generic_form_nation
		days_mission_timeout = 10
		visible = {
			original_tag = BLI
			if = {
				limit = { 
					THIS = { is_subject_of = BLI } 
				}
			}
		}
		allowed = {
			original_tag = BLI
			if = {
				limit = { 
					THIS = { is_subject_of = BLI } 
				}
			}
		}
		available = {
			has_country_flag = BLI_crisis_fall_of_empire
		}
		fire_only_once = yes
		timeout_effect = {
			every_subject_country = {
				limit = { is_subject_of = BLI }
				end_puppet = PREV
				leave_faction = yes
				declare_war_on = {
					target = PREV
					type = annex_everything
				}
			}
			hidden_effect = {
				news_event = {
					id = news.6
					days = 5
				}
			}
		}
	}
}