crisis = {
	fall_of_empire = {
		icon = generic_form_nation
		days_mission_timeout = 10
		allowed = {
			always = yes
		}
		available = {
			has_country_flag = BLI_crisis_fall_of_empire
		}
		visible = {
			tag = BLI
			if = {
				limit = { 
					THIS = { is_subject_of = BLI } 
				}
			}
		}
		timeout_effect = {
			if = {
				limit = { 
					ROOT = { is_subject_of = BLI} 
				}
				BLI = {
					end_puppet = FROM
				}
				declare_war_on = {
					target = BLI
					type = annex_everything
				}
			}
		}
	}
}