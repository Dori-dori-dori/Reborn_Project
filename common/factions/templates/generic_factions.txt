# Runtime-safe fallback for the single-country blank world. Country-specific manifests,
# goals, templates and state filters are intentionally omitted.
faction_template_generic = {
	name = generic_faction_name
	can_leader_join_other_factions = yes
	icon = GFX_faction_logo_generic
	visible = { always = yes }
	available = { always = yes }
	goals = { }
	default_rules = {
		joining_rule_same_ideology
		joining_rule_has_no_war
		change_leader_rule_industry
		war_declaration_rule_faction_leader_only
		call_to_war_rule_faction_leader_only
	}
}
