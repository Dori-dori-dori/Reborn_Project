change_leader_rule_industry = {
	type = change_leader_rules
	trigger = { has_industry_to_become_leader = yes }
	ai_will_do = {
		base = 0
		modifier = {
			add = 5
			is_faction_leader = yes
			OR = {
				industrial_complex > 100
				arms_factory > 100
			}
		}
	}
}

change_leader_rule_influence = {
	type = change_leader_rules
	trigger = {
		has_enough_influence_for_leadership = yes
	}
}
