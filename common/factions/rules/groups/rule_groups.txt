rule_group_ideology = {
	rules = { joining_rule_same_ideology }
}

rule_group_war_relation = {
	rules = { joining_rule_has_no_war }
}

rule_group_war_declaration_declarer = {
	rules = { war_declaration_rule_faction_leader_only }
}

rule_group_call_to_war = {
	rules = { call_to_war_rule_faction_leader_only }
}

rule_group_leader_industry = {
	rules = { change_leader_rule_industry }
}

INFLUENCE_SORT = {
	rules = { change_leader_rule_influence }
}
