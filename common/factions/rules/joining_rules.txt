joining_rule_same_ideology = {
	type = joining_rules
	ai_will_do = { base = 0 }
	trigger = { has_government = FROM }
}

joining_rule_has_no_war = {
	type = joining_rules
	ai_will_do = {
		base = 0
		modifier = {
			add = 3
			has_government = neutrality
			has_war = no
		}
	}
	trigger = { has_war = no }
	modifier = { improve_relations_maintain_cost_factor = -0.1 }
}
